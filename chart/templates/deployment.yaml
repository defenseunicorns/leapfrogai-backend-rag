apiVersion: apps/v1
kind: Deployment
metadata:
  name: rag-deployment
spec:
  replicas: {{ .Values.rag.replicas }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: rag
  template:
    metadata:
      labels:
        app: rag
    spec:
      containers:
        - name: rag-container
          image: ghcr.io/defenseunicorns/leapfrogai/rag:{{ .Values.image.lfaiRagTag }}
          imagePullPolicy: Always
          env:
          - name: OPENAI_API_BASE
            value: {{ .Values.env.openai_api_base }}
          - name: OPENAI_API_KEY
            value: {{ .Values.env.openai_api_key }}
          - name: MODEL
            value: {{ .Values.env.model }}
          - name: TEMPERATURE
            value: {{ .Values.env.temperature }}
          - name: CONTEXT_WINDOW
            value: {{ .Values.env.context_window }}
          - name: MAX_OUTPUT
            value: {{ .Values.env.max_output }}
          - name: CHUNK_SIZE
            value: {{ .Values.env.chunk_size }}
          - name: OVERLAP_SIZE
            value: {{ .Values.env.overlap_size }}
          - name: RESPONSE_MODE
            value: {{ .Values.env.response_mode }}
          - name: SSL_VERIFICATION
            value: {{ .Values.env.ssl_verification }}
          ports:
            - containerPort: 8000
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          securityContext:
            runAsUser: 65532
            runAsGroup: 65532
            fsGroup: 65532



